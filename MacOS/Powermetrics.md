Powermetrics
=============================

Show Help
```
powermetrics -h
```

Recipes
-------------------------------

Check CPU temperature
```
sudo powermetrics --samplers smc | grep -i "CPU die temperature"
```